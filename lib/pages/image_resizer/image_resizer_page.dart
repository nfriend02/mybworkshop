import 'package:flutter/material.dart';

import '../../features/image_resizer/ui/image_resizer_view.dart';

class ImageResizerPage extends StatelessWidget {
  const ImageResizerPage({super.key});

  @override
  Widget build(BuildContext context) => const ImageResizerView();
}
